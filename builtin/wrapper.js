//wrapper object
//Convert raw values to related built-in objects as needed.
const number = 123; // number's raw value
console.log(number.toString()); //
console.log(number); // number's raw value

const text = "text"; // String char raw value
console.log(text);
text.length; //String object
text.trim();
